// 2 DIMENSIONAL ARRAY IN JAVA
// MUTABLE
public class Two_dim {
  public static void main(String[] args) {
    // Declare and initialize a 2D array
    int[][] arr = {
        {1, 2, 3},
        {4, 5, 6},
        {7, 8, 9}
    };

    System.out.println("Element at arr[0][0]: " + arr[0][0]); // Output: 1

  }  
}

// RANDOM NUMBER GENERATION IN JAVA
// int num = (int)Math.random()*100; // 0-99

// int nums[][] = new int[3][]; //  ******jagged array


// A jagged array (also known as an array of arrays) is an array where each element is itself an array, 
// but the arrays can have different lengths. Unlike a regular multi-dimensional array, a jagged array 
// doesn’t require each sub-array to have the same number of elements.

